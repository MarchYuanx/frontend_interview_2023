HTTP 1.0
- 支持 get post

HTTP 1.1
- 支持 PUT DELETE, 适用于 restful api
- 增加缓存策略 cache-control E-tag 等等
- 支持长连接 Connection: keep-alive 一次 TCP 连接多次请求
- 支持断点续传


HTTP 2.0
- 支持压缩 header 减少体积
- 多路复用 一次 TCP 连接可以多个 HTTPS 并发请求
- 服务端推送



HTTP 3.0